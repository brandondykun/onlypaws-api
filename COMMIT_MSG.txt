feat(moderation): add profile blocking with API and app-wide filtering

- Add Block model (blocker, blocked), migration, and block_utils with
  get_blocked_profile_ids/are_profiles_blocking (cached per request).
- Add block/unblock/list-blocked API; on block, remove follow/follow-request
  and notifications between the two profiles.
- Filter blocked users from: comments, replies, followers, following,
  follow-requests, notifications; reject follow when blocking.
- Exclude blocked profiles from feed, explore, similar, saved, and tagged
  posts; return 404 for profile posts and single post when blocked.
- Post interaction and image tags respect blocks; profile detail exposes
  is_blocked and hides follow state/can_view_posts when blocked; search
  excludes blocked profiles.
- Add Block admin (read-only) and test_block_api tests.
