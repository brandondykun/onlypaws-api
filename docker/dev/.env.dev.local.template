# Django env variables
DEBUG=True
DEV=True
DJANGO_ENV=dev

# JWT env variables
ACCESS_TOKEN_LIFETIME= # minutes
REFRESH_TOKEN_LIFETIME= # days

# DB env variables
DB_HOST=
DB_NAME=
DB_USER=
DB_PASSWORD=

# AWS/R2 env variables
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=auto
# S3 API endpoint for uploads and signed operations
AWS_S3_ENDPOINT_URL=
# Public URL for read-only image URLs (e.g. R2 public bucket or custom domain; no signing)
AWS_S3_ENDPOINT_PUBLIC_URL=

# Email env variables
EMAIL_HOST=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=
EMAIL_PORT=

# Media domain for images (optional, defaults to S3 bucket URL)
# MEDIA_DOMAIN=
